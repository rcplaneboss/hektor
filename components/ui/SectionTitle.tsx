import React from 'react'

const SectionTitle = ({title}) => {
  return (
    <div>
       <h2 className="font-sans text-3xl text-p1 font-semibold">
          {title}
        </h2>
    </div>
  )
}

export default SectionTitle
